const rd = require('readline-sync')


let bancoDeDados = []

function adicionarAbrigo(){
    ...

let abrigoCidade = rd.question("Qual a cidade em que se encontra? ")
abrigoCidade.toLowerCase()  
 
...
bancoDeDados.push({
   ...
    Cidade: abrigoCidade,
    ...

})
}

let cidade
function exibirMenu(){
switch (opcao){
    case "3":
        if(bancoDeDados.length == 0){console.log("Não há nenhum abrigo.")}
        else{
        let procurarAbrigo = rd.question("Qual a sua cidade? ")
        if(procurarAbrigo == 0){console.log("Nenhum abrigo encontrado nessa cidade.")}
        else{
                  function encontrar(abrigo){
                cidade = abrigoCidade.adicionarAbrigo
              return abrigo === cidade;}
              const abrigosEncontrados = cidade.filter(encontrar);
              console.log (abrigosEncontrados)
            }
        }
    }
}
